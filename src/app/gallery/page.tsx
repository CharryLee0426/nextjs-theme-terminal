import { GalleryPageClient } from "./GalleryPageClient";

export const metadata = {
  title: "Gallery",
  description: "Photo gallery and timeline on CharryLee’s terminal site.",
};

export default function GalleryPage() {
  return <GalleryPageClient />;
}
